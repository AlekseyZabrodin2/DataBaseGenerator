<UserControl
    x:Class="DataBaseGenerator.UI.Wpf.UserControls.WorkListTableUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DataBaseGenerator.UI.Wpf.UserControls"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Height="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid
            Name="WorkListTable"
            Grid.Column="1"
            Grid.ColumnSpan="3"
            Background="{DynamicResource MahApps.Brushes.Accent}">

            <DataGrid
                Name="ViewAllWorkList"
                Margin="5"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                EnableColumnVirtualization="True"
                EnableRowVirtualization="True"
                IsReadOnly="True"
                ItemsSource="{Binding AllWorkLists}"
                ScrollViewer.IsDeferredScrollingEnabled="True"
                VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                VirtualizingPanel.ScrollUnit="Pixel"
                VirtualizingPanel.VirtualizationMode="Recycling">

                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding ID_Patient}"
                        Header="ID пациента" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding Modality}"
                        Header="Модальность" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding StationAeTitle}"
                        Header="AE_Title" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding State}"
                        Header="Состояние" />
                    <DataGridTextColumn
                        Width="150"
                        Binding="{Binding StudyDescription}"
                        Header="Описание" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding CreateDate, StringFormat='dd.MM.yyyy'}"
                        Header="Дата добавления" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding CreateTime}"
                        Header="Время добавления" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding ProcedureStepStartDateTime, StringFormat='dd.MM.yyyy'}"
                        Header="Дата процедуры" />
                    <DataGridTextColumn
                        Width="150"
                        Binding="{Binding PerformingPhysiciansName}"
                        Header="Исполнит. врач" />
                    <DataGridTextColumn
                        Width="150"
                        Binding="{Binding ReferringPhysiciansName}"
                        Header="Направ. врач" />
                    <DataGridTextColumn
                        Width="150"
                        Binding="{Binding RequestingPhysician}"
                        Header="Запрос. врач" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Grid.Row="1" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid Name="TextBoxWorkListCount" Margin="5">

                    <TextBlock
                        Name="WlCount"
                        Margin="5"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="15"
                        FontWeight="Bold"
                        Text="WorkList Count" />

                    <TextBox
                        Name="WorkListCount"
                        Width="100"
                        Height="50"
                        Margin="0,0,30,0"
                        HorizontalAlignment="Right"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Background="{DynamicResource MahApps.Brushes.Button.Border}"
                        BorderBrush="Black"
                        FontWeight="Bold"
                        Text="{Binding SetWorkListCount, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>

                <Grid
                    Name="TextBoxModality"
                    Grid.Column="1"
                    Margin="5">

                    <TextBlock
                        Name="Modality"
                        Margin="40,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="15"
                        FontWeight="Bold"
                        Text="Modality" />

                    <ComboBox
                        Name="ModalityBox"
                        Width="100"
                        Height="50"
                        Margin="0,0,30,0"
                        HorizontalAlignment="Right"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Background="{DynamicResource MahApps.Brushes.Button.Border}"
                        BorderBrush="Black"
                        FontWeight="Bold"
                        ItemsSource="{Binding ModalityRules}"
                        SelectedItem="{Binding SelectModality, Mode=TwoWay}" />
                </Grid>

                <Grid
                    Name="TextBoxAeTitle"
                    Grid.Column="2"
                    Margin="5,0,10,0">

                    <TextBlock
                        Name="AeTitle"
                        Margin="30,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="15"
                        FontWeight="Bold"
                        Text="AE Title" />
                    <TextBox
                        Name="AeTitleBox"
                        Width="150"
                        Height="50"
                        Padding="10,0"
                        HorizontalAlignment="Right"
                        HorizontalContentAlignment="Left"
                        VerticalContentAlignment="Center"
                        mah:TextBoxHelper.Watermark="Enter title:"
                        Background="{DynamicResource MahApps.Brushes.Button.Border}"
                        BorderBrush="Black"
                        FontWeight="Bold"
                        Text="{Binding SetAeTitle, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid
                    Name="ButtonAddWorkList"
                    Grid.Column="0"
                    Margin="5">

                    <Button
                        Name="AddWorkList"
                        Width="120"
                        Height="50"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Left"
                        Command="{Binding AddWorkListCommand}"
                        Content="Add WorkList" />

                    <Button
                        Name="UpdateWorkList"
                        Grid.Column="0"
                        Width="120"
                        Height="50"
                        Margin="0,0,10,0"
                        HorizontalAlignment="Right"
                        Command="{Binding RefreshWorkListCommand}"
                        Content="Update WorkList" />
                </Grid>

                <Grid
                    Name="ButtonDeleteFirstWorkList"
                    Grid.Column="2"
                    Margin="5,0,10,0">

                    <Button
                        Name="DeleteFirstWorkList"
                        Width="120"
                        Height="50"
                        Margin="15,0,0,0"
                        HorizontalAlignment="Left"
                        Command="{Binding DeleteFirstWorkListCommand}"
                        Content="Delete First WL" />

                    <Button
                        Name="DeleteAllWorkList"
                        Width="120"
                        Height="50"
                        HorizontalAlignment="Right"
                        Command="{Binding DeleteAllWorkListCommand}"
                        Content="Delete All WL" />
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
