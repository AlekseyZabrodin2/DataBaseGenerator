<UserControl
    x:Class="DataBaseGenerator.UI.Wpf.UserControls.AddPatientUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DataBaseGenerator.UI.Wpf.UserControls"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:DataBaseGenerator.UI.Wpf.ViewModel">


    <Grid Name="AddPatients">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
            BorderThickness="5">
            <StackPanel>

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Patient ID *" />
                <TextBox Margin="5" Text="{Binding AddIdPatient, Mode=TwoWay}" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Surname: *" />
                <TextBox Margin="5" Text="{Binding AddFamily, Mode=TwoWay}" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Name: *" />
                <TextBox Margin="5" Text="{Binding AddName, Mode=TwoWay}" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Middle name: *" />
                <TextBox Margin="5" Text="{Binding AddMiddleName, Mode=TwoWay}" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Birthdate:" />
                <mah:DateTimePicker
                    Margin="5"
                    Culture="ru"
                    SelectedDateFormat="Short"
                    SelectedDateTime="{Binding PatientBirthDate, Mode=TwoWay}"
                    SelectedTimeFormat="Long" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Gender:" />
                <ComboBox
                    Margin="5"
                    ItemsSource="{Binding Gender}"
                    SelectedItem="{Binding SelecedGender}" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Address:" />
                <TextBox Margin="5" Text="{Binding AddAdress, Mode=TwoWay}" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Place of work" />
                <TextBox Margin="5" Text="{Binding AddWorkPlase, Mode=TwoWay}" />

                <TextBlock
                    Margin="5,5,0,0"
                    FontWeight="DemiBold"
                    Text="Additional information:" />
                <TextBox
                    Height="217"
                    Margin="5"
                    Text="{Binding AddInfo, Mode=TwoWay}"
                    TextWrapping="Wrap" />

            </StackPanel>
        </Border>

        <Button
            Grid.Row="1"
            Width="70"
            Height="50"
            Margin="50,10,0,0"
            HorizontalAlignment="Left"
            Command="{Binding AddOnePatientCommand}"
            Content="Add" />
        <Button
            Grid.Row="1"
            Width="70"
            Height="50"
            Margin="0,10,50,0"
            HorizontalAlignment="Right"
            Command="{Binding CancelAddPatientCommand}"
            Content="Cancel" />
    </Grid>
</UserControl>
